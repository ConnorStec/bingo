{
  "name": "bingo-party",
  "version": "1.0.0",
  "description": "A mobile-first web app for playing bingo with family",
  "license": "ISC",
  "author": "",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "server": "npm run dev --workspace=backend",
    "ui": "npm run dev --workspace=frontend"
  }
}
